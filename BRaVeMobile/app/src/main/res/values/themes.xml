<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BRaVeMobile" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="popupMenuStyle">@style/Widget.Material3.PopupMenu</item>
        <item name="android:popupMenuStyle">@style/Widget.Material3.PopupMenu</item>

        <!-- Customize your theme here. -->
    </style>

    <style name="RoundedCard">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">14dp</item>
    </style>

    <!-- Base application theme. -->
    <!--<style name="Base.Theme.BRaVeMobile" parent="Theme.Material3.DayNight.NoActionBar">-->
    <!-- Customize your light theme here. -->
    <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    <!--</style>-->

    <style name="Base.Theme.BRaVeMobile" parent="Theme.Material3.DayNight">
        <!-- no NoActionBar here -->
    </style>

    <!-- res/values/themes.xml -->
    <style name="Theme.MyApp.Splash" parent="Theme.Material3.DayNight">
        <!-- Background color (same blue from your image) -->
        <item name="windowSplashScreenBackground">#003E7E</item>

        <!-- Your logo (place the file in res/drawable, e.g. brave_logo.png) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_braveiom_logo</item>

        <!-- After splash, switch to main app theme -->
        <item name="postSplashScreenTheme">@style/Theme.BRaVeMobile</item>
    </style>


</resources>